name: CI

on:
  push:
    branches: [ master, main ]
  pull_request:
    branches: [ master, main ]

env:
  CARGO_TERM_COLOR: always

jobs:
  test:
    name: Test on ${{ matrix.os }} / ${{ matrix.rust }} / ${{ matrix.features }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest]
        rust: [stable]
        # Define the full matrix of features to cover all environments
        features:
          # 1. Base PQC (no_std, no_alloc) - Uses default features = []
          - "" 
          # 2. PQC + AES-GCM (no_std, no_alloc)
          - "aes-gcm"
          # 3. PQC + Alloc + AES-GCM (no_std, alloc)
          - "alloc,aes-gcm"
          # 4. PQC + Std + AES-GCM (Full, includes alloc/std tests)
          - "std,aes-gcm"

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: ${{ matrix.rust }}

      - name: Build C dependencies
        run: |
          # The pqcrypto crates require C dependencies to be built first.
          # This is typically handled by the Rust build system, but
          # we ensure all C tools are available for cross-compilation contexts.
          sudo apt-get update
          sudo apt-get install -y gcc

      - name: Run Tests
        # We handle the feature matrix logic here:
        # If matrix.features is the empty string "", we run `cargo test`.
        # Otherwise, we run with features enabled, checking for std/alloc flags.
        
        run: |
          if [ "${{ matrix.features }}" == "" ]; then
            # Default configuration (no_std, no_alloc, PQC only)
            echo "Running: cargo test"
            cargo test
          elif [ "${{ matrix.features }}" == "aes-gcm" ]; then
            # PQC + AES-GCM (no_std, no_alloc)
            echo "Running: cargo test --no-default-features --features aes-gcm"
            cargo test --no-default-features --features aes-gcm
          else
            # Handles alloc, std, and combinations
            echo "Running: cargo test --features ${{ matrix.features }}"
            cargo test --features ${{ matrix.features }}
          fi
